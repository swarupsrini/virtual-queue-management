import { makeStyles } from "@material-ui/core/styles";

export default makeStyles({
  title: {
    marginTop: "40px",
    textAlign: "center",
    fontSize: "50pt",
    color: "#4455BB"
  },
  subtitle: {
    textAlign: "center",
    fontSize: "16pt"
  }
});